{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["node"],
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_20"]
        },
        "install": {
          "cmds": ["cd admin-ui && npm install"]
        },
        "build": {
          "cmds": ["cd admin-ui && npm run build"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "cd admin-ui && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
